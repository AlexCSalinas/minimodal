[pytest]
# tests/ also holds the integration scripts (tests/test_*.py), which boot the
# real orchestrator binary and are driven by `make test-integration`. Only the
# unit suite is collected here.
testpaths = tests/unit
addopts = -q
